export const routineVariations = {
	tanning: Array.from({ length: 12 }, (_, i) => `Gentle Tanning Variant ${i + 1}`),
	postSun: Array.from({ length: 12 }, (_, i) => `Post-Sun Variant ${i + 1}`),
	winter: Array.from({ length: 11 }, (_, i) => `Winter Hydration Variant ${i + 1}`),
	hydration: Array.from({ length: 15 }, (_, i) => `Hydration Variant ${i + 1}`),
	allergySafe: Array.from({ length: 12 }, (_, i) => `Allergy-Safe Variant ${i + 1}`),
};